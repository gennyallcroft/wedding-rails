
<div class="sign-up-text"> Please see your RSVP for the wedding below. </div>

<center><div class="form-background" >
<div class="form-group">
  <label for="guest_name">Guest Name</label>
  <div class="guest-name-text"> <%= current_user.first_name %> <%= current_user.surname %> </div>
</div>
<div class="form-group">
<% if @response.rsvp === "No" %>
<div class="guest-name-text"> We are sorry to hear that you are not able to make it to the wedding </div>
<% else %>
<div class="guest-name-text"> We are delighted that you are coming to the wedding </div>
<% end %>
</div>

<div class="form-group">
<% if @response.rsvp === "Yes" %>
  <% if @response.dietary_requirements === "None" %>
  <div class="guest-name-text"> You do not have any dietary requirements </div>
  <% else %>
  <div class="guest-name-text"> Your dietary requirements are: <%= @response.diet_details %> </div>
  <% end %>
<% end %>
</div>


<div class="form-group">
<% if @response.text != "" %>
  <label for="guest_name">Your extra comments: </label>
  <div class="guest-name-text"> <%= @response.text %> </div>
<% end %>
</div>

<div class="form-group">
    <div class="guest-name-text">You can edit your response up to 1 May 2021 by clicking the button below </div>
</div>

<center>
<div class="btn btn-secondary">
<%=  link_to 'Edit', edit_response_path(@response) %>
</div>
</center>